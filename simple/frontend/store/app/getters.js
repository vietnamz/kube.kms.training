export default {
    getImage(state) {
        return state.image;
    },

    getColor(state) {
        return state.color;
    },

    getDrawer(state) {
        return state.drawer;
    },

    getLoginState(state) {
        return state.loginState;
    }
};
